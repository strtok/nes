var assert = require('assert');
var rom = require('../rom')
var nestest = require('./nestest_rom');

describe('rom', () => {
    describe('load', () => {
        it('xxx', function() {
            var romData = nestest.byteArray();
            romData.toString();
        });
    });
});